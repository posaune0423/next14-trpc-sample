// import { trpc } from "@/lib/trpc";

const ServerTest = () => {
  // const { data } = trpc.userById.useQuery(1);
  console.log("server");
  // console.log(data);

  return <></>;
};

export default ServerTest;
